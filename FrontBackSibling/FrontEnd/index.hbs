<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1.0">

    <th:block th:if=${storeInfo}><title th:text=${storeInfo.title}>test-project</title>
      <th:block th:each="meta : ${storeInfo.seoMeta}">
        <meta th:attr="name=${meta.key}, content=${meta.value}">
      </th:block>
    </th:block>
  </head>
  <body>
    <div id="app"></div>
    <!-- built files will be auto injected -->
  </body>
</html>
